# HZ keys

```bat
FriendsKeyGen.bat init
FriendsKeyGen.bat key --days 30 --note "name"
FriendsKeyGen.bat list
FriendsKeyGen.bat revoke 1
```

Keys look like `HZ1....`. Rebuild `HZ-Retail` after `init` or `revoke`.
Never commit `friends-private.bin`.
